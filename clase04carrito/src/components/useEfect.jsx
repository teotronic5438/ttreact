// import { use, useEffect } from "react";

// useEffect(() => {}, []); // Se ejecuta una vez al cargar el componente
// useEffect(() => {}, [variable]); // Se ejecuta cada vez que cambia la variable 
// useEffect(() => {}, [delendencias]); // Se ejecuta cada vez que cambia la variable de dependencias